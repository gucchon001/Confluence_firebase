{
  "_comment": "注意: このファイルは現在使用されていません。AIモデル設定は src/config/ai-models-config.ts で一元管理されています。",
  "confluence": {
    "baseUrl": "${CONFLUENCE_BASE_URL}",
    "email": "${CONFLUENCE_USER_EMAIL}",
    "apiToken": "${CONFLUENCE_API_TOKEN}",
    "spaceKey": "${CONFLUENCE_SPACE_KEY}",
    "maxPages": null,
    "batchSize": 5,
    "outputDir": "data/confluence-extraction-v2"
  },
  "llm": {
    "apiKey": "${GEMINI_API_KEY}",
    "_comment_model": "注意: モデル設定は src/config/ai-models-config.ts の GeminiConfig で一元管理されています。この値は参考用です。",
    "model": "gemini-2.5-flash",
    "batchSize": 50,
    "outputDir": "data/llm-extraction-v2",
    "maxRetries": 3,
    "delayBetweenRequests": 0
  },
  "validation": {
    "outputDir": "data/validation-v2"
  },
  "pipeline": {
    "steps": [
      "confluence",
      "llm"
    ],
    "outputDir": "data/domain-knowledge-v2"
  }
}