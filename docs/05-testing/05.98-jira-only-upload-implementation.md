# Jiraのみのアップロード機能の実装

## 実施日
2025-01-28

## 目的
Jiraの改修時にConfluenceの本番データに影響を与えないよう、Jiraテーブルのみをアップロードできる機能を追加。

## 実装内容

### 1. テーブルフィルター機能の追加

`scripts/upload-production-data.ts`に環境変数`UPLOAD_TABLE_FILTER`を追加：

```typescript
// テーブルフィルター（環境変数で指定可能）
const tableFilter = process.env.UPLOAD_TABLE_FILTER;
if (tableFilter) {
  const originalCount = tableNames.length;
  tableNames = tableNames.filter(name => name === tableFilter);
  console.log(`\n🔍 テーブルフィルター適用: "${tableFilter}"`);
  // ...
}
```

### 2. Jira専用アップロードスクリプトの作成

`scripts/upload-jira-production-data.ts`を作成：

```bash
npm run upload:jira-production-data
```

このスクリプトは、`UPLOAD_TABLE_FILTER=jira_issues`を設定して`upload-production-data.ts`を実行します。

## 使用方法

### Jiraテーブルのみをアップロード

```bash
# 方法1: 専用スクリプトを使用（推奨）
npm run upload:jira-production-data

# 方法2: 環境変数を使用
UPLOAD_TABLE_FILTER=jira_issues npm run upload:production-data
```

### Confluenceテーブルのみをアップロード

```bash
UPLOAD_TABLE_FILTER=confluence npm run upload:production-data
```

### 全テーブルをアップロード

```bash
# 環境変数を指定しない（従来通り）
npm run upload:production-data
```

## 安全性

### ✅ Confluenceへの影響なし

- Jiraテーブルのみをアップロードするため、Confluenceの本番データは変更されません
- テーブルフィルターが明示的に指定されるため、誤アップロードのリスクが低減されます

## 次のステップ

1. **Jiraテーブルのアップロード**
   ```bash
   npm run upload:jira-production-data
   ```

2. **アップロード後の確認**
   ```bash
   npm run test:jira-chunking:production
   ```

3. **本番環境での動作確認**
   - アップロード後、本番環境でチャンク分割機能が正常に動作することを確認

